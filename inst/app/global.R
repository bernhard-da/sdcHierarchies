library(shiny)
library(data.tree)
library(DiagrammeR)
library(shinyjs)
library(sdcTable)

#
# d <- create_node("Total")
# d <- add_nodes(d, reference_node = "Total", letters[1:3])
# d <- add_nodes(d, reference_node = "a", c("a1","a2","a3"))
# d <- add_nodes(d, reference_node = "a1", c("a11","a12","a13"))
# d

